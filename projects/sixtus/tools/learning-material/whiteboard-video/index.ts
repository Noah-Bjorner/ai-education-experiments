export const generateWhiteboardVideo = async () => {
    // 1. scenses and plan
    // 2. media
    // 3. animation — seek/trigger the SVG drawing from ../whiteboard/animation
    // 4. export
    return;
};

